export enum WsEvent {
  AUTH = 'auth/token',
  BIDS = 'bids',
  BID = 'bid',
  MONITOR = 'monitor',
  MONITOR_STATUS = 'monitor/status',
  MONITOR_DELETE = 'monitor/delete',
  WALLET = 'wallet',
  METRICS = 'metrics',
  ERROR = 'error',
}
